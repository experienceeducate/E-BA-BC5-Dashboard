# Dev/test dependencies. Not part of the production Docker image (Dockerfile
# only installs requirements.txt) — install alongside it locally and in CI:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
